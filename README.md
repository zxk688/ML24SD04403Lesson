# 机器学习课程作业
# 任务说明
```
（1）基于机器学习的蛋白肽序列活性分类任务，利用提供的训练集，设计模型，进行模型训练，在测试集进行预测；
（2）用python实现，可以使用的python库包括：numpy、pandas、sklearn、pytorch等；
（3）独立完成或者最多四人组队，提交代码和说明文档。代码py文件存储，能够直接运行，如用机器学习，保留训练和测试代码；如用深度学习，还要保存模型权重。如进行了调参，只需保留最优参数及对应的结果即可；
（4）提交的文档内容包括：小组成员及学号，使用的python库，采用的数据处理（如有）、模型和测试精度，精度计算采用sklearn库的balanced_accuracy_score(y_true, y_pred)函数；
（5）5月12日之前，发送到邮箱zhangxiaokang@wust.edu.cn，5月16日课上请同学讲解；
（6）成绩按照模型设计和精度情况给分，不提交的、提交的代码无法运行、抄袭的，没有平时成绩；
（7）测试集精度不应低于0.62。
```
# 数据说明
```
  （1）所有数据都保存在ACE_features_320_dimension.csv，共1384个序列，每个序列有320个特征
  （2）分成A和B两类，见【group】列
  （4）按照4：1的比例划分训练集和测试集，见【split】列
```
